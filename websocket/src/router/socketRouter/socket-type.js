const LOGIN = "login"

const OFFLINE = "offline"
const SYSTEM_MESSAGE = "system_message"
const PLAYER_MESSAGE = "player_message"

const JOIN_GAME = "join_game"
const CHANGE_GAME_INFO = "change_game_info"
const CREATE_PIECE = "create_piece"

const REMARK = "remark"

module.exports = {
  LOGIN,
  OFFLINE,
  SYSTEM_MESSAGE,
  PLAYER_MESSAGE,
  JOIN_GAME,
  CHANGE_GAME_INFO,
  CREATE_PIECE,
  REMARK
}